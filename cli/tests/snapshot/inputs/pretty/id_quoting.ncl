# capture = 'stdout'
# command = ['pprint-ast']
{
  "a field" = 1,
  "Number" = 1,
  bar = 'if,
  foo = bar |> match { 'if => 1, '"has space" => 0 },
  has_space = '"hi there",
  annotated | [| 'if, 'then, '"has space" |],
}."a field"
