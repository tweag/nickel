# capture = 'stdout'
# command = ['export']
# extra_args = ['--format', 'yaml-documents']
[
  {
    a_record = {
      even = {
        more = {
          nested = "record"
        }
      }
    }
  },
  1,
  { another_record = "hi" },
]
